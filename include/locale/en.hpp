#pragma once

#include <string>
#include <array>

#include "locale.hpp"

const std::array<std::string, __LOCALE_SIZE__> locale_en = {
    "Calling [built-in]{}()",
    "Return with {}",
    "Calling :{}()",
    "Checking if expression with {} for the {} {}",
    "No valid statement found, running else body",
    "Exiting with {}",
    "Initializing variant {}",
    "Inline array definition found",
    "Inline access to {} with index {}",
    "Evaluating {} {} {}"
};